{
  "name": "@jeroenpeeters/blog-design-system",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:prod": "parcel build src/index.scss --no-cache --no-source-maps --dist-dir ../docs/assets",
    "build": "parcel build src/index.scss --no-cache --dist-dir ../docs/assets",
    "watch": "parcel watch src/index.scss --no-cache --dist-dir ../docs/assets"
  },
  "author": {
    "name": "Jeroen Peeters",
    "email": "contact@jeroenpeeters.be"
  },
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "2.9.3",
    "parcel": "^2.9.3"
  },
  "browserslist": [
    ">= 0.5%",
    "last 2 major versions",
    "not dead",
    "not ie > 0"
  ],
  "dependencies": {
    "normalize.css": "^8.0.1",
    "sanitize.css": "^13.0.0"
  }
}
